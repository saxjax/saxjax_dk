import { TitleFromSquareBracketsPipe } from './title-from-square-brackets.pipe'

describe('TitleFromSquareBracketsPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleFromSquareBracketsPipe()
    expect(pipe).toBeTruthy()
  })
})
